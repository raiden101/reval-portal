const admin = require('./admin');
const student = require('./student');
const subject = require('./subject');
const usn_booklet = require('./usn_booklet');
const reval = require('./reval');

module.exports = {
  admin,
  student,
  subject,
  usn_booklet,
  reval
}